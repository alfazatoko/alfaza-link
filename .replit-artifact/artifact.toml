kind = "web"
previewPath = "/"
title = "Alfaza Link"
version = "1.0.0"
id = "artifacts/alfaza-link"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25235

[services.development]
run = "pnpm --filter @workspace/alfaza-link run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/alfaza-link", "run", "build" ]
publicDir = "artifacts/alfaza-link/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25235"
BASE_PATH = "/"
